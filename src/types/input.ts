export type FormState = {
  email: string;
  code: string;
  pw: string;
  checkPw: string;
  name: string;
  phone: string;
  subPhone: string;
  [key: string]: string;
};

export type URLFormState = {
  youtube: string;
  blog: string;
  insta: string;
  tiktok: string;
};